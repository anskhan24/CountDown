This is a Countdown App that allows user to create a countdown for the entered time,

It is created with simple HTML, CSS and JavaScript

The timer has to set inside the JavaScript file in the endDate. 